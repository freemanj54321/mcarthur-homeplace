/**
 * Version of the content contract (schemas + document shapes) exposed to the
 * website and any external consumer (e.g. the future mobile app via the
 * content API). Bump when the public content shape changes.
 */
export const CONTENT_SCHEMA_VERSION = '1.0.0'
